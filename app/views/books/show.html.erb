  <div class="text-center"><h1><%= @book.title %></h1>

  <h2><%=@book.author%></h2>
  <h2><%="price: $"%><%=@book.price%></h2>

  <% if @book.photo? %>
  <%= cl_image_path @book.photo.path, width: 200, height: 300, crop: :fill %>
<% end %>
<%="Reviews for: "%><%= link_to @book.user.email, user_path(@book.user) %>


  <%= link_to "home", books_path %>
  </div>

<!-- books.sold -->
<!-- books.photo -->
