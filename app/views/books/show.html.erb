  <h1><%= @book.title %></h1>
  <h2><%=@book.author%></h2>
  <h2><%=@book.price%></h2>

  <% if @book.photo? %>
  <%= cl_image_tag @book.photo.path, width: 200, height: 300, crop: :fill %>
<% end %>
  <%= link_to "home", books_path  %>

<!-- books.sold -->
<!-- books.photo -->
